# Webapp (React + Vite)

## Run

```bash
cd webapp
npm install
npm run dev
```

Default app URL: http://localhost:5173

Store API base URL is `http://localhost:9000` by default.

Optional env file:

```bash
cp .env.example .env
```
